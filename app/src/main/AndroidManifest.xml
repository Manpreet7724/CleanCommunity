<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cleanup.cleanapp.cleancommunity">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:allowBackup="true"
        android:fullBackupContent="false"

        >
        <activity
            android:name=".MainActivity"
            android:usesCleartextTraffic="true"
            android:screenOrientation="fullSensor" >
                       <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".startup2" android:screenOrientation="fullSensor"> </activity>
        <activity android:name=".login" android:screenOrientation="fullSensor"> </activity>
        <activity android:name=".signUp" android:screenOrientation="fullSensor"> </activity>


    </application>

</manifest>